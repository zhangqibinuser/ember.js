'use strict';

/*
// import { EMBER_SVELTE_APPLICATION } from '@ember/canary-features';
// import { assert } from '@ember/debug';
// import { DEBUG } from '@glimmer/env';
// import { Registry } from '@ember/-container';
// import { ILocation } from 'ember-routing';

// export interface BootOptions {
//   rootElement: Element;
//   registry: Registry;
//   location: ILocation;
// }

// // Responsibilities:
// // Set up registry
// // Run initializers
// // initialize and kick off router
// // set root element
// let CoreApplication;

// if (EMBER_SVELTE_APPLICATION) {
//   function validateBootOptions(options: BootOptions) {
//     assert('Options passed to boot() must contain a rootElement.', options.rootElement);
//   }

//   CoreApplication = class CoreApplication {
//     private _booted = false;
//     private _bootPromise: Promise<CoreApplication>;

//     boot(options: BootOptions) {
//       if (this._bootPromise) { return this._bootPromise; }
//       this._bootPromise = this._boot(options);
//     }

//     async _boot(options: BootOptions) {
//       if (DEBUG) {
//         validateBootOptions(options);
//       }

//       if (this._booted) {
//         throw new Error('Application is already booted.');
//       }

//       this.setupRegistry();
//     }

//     setupRegistry() {}
//   };
// }

// export default CoreApplication;

// // MAYBE SUPPORT
// // log libraries
// //
// // TO DEPRECATE
// // string root element
// // event delegation
// // custom events
// // App.reset()
// // deferReadiness()
// // advanceReadiness()
// // waiting for DOM ready
// // general coupling to the DOM
// //
// // FIGURE OUT
// // what is the action story
*/
export default {};
